# Foodtruck Apollo Frontend

The frontend is a React 19 application built with Vite.

Run commands from the repository root:

```bash
make frontend
make build
```

Environment variables are documented in `.env.example`. Full project setup and
deployment instructions are in the root `README.md`.
